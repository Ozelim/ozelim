import React from 'react'
import { getImageUrl } from 'shared/lib'

export const Card = ({partner}) => {
  return (
    <div>
      {/* <img src={getImageUrl(partner, partner.)} /> */}
      <div></div>
      <div></div>
    </div>
  )
}
